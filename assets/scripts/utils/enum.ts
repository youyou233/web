export enum ResType {
    main
}

export enum GameStatue {
    pause,
    start
}

export enum GroupType {
    player = 1,
    enemy = 2
}

export enum BulletType {
    fly = 1,
    light = 2,
    track = 3
}

export enum DirType {
    up = 1,
    down = 2,
    left = 3,
    right = 4
}
